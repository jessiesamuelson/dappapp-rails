# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 127aaea1a172f8da6caad47ff9238c98f33e04f1a2a7d3f47da79a8bce43e8c172cb06e88cdd77c574e6c3864e24f95bd8e20f4f8bdf39ba5cdf80eeb2e37870


test:
  secret_key_base: ba3baf326de3ed292e900e17ac41197a17b7541ac072fdebeb8a0f3e8be9d89b8015754f59fcb9dac40a300afcbea18e81dd2305cf67aa7ecde82f8734783dbc

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  consumer_key: <%= ENV["CONSUMER_KEY"] %>
  consumer_secret: <%= ENV["CONSUMER_SECRET"] %>
  access_token: <%= ENV["ACCESS_TOKEN"] %>
  access_token_secret: <%= ENV["ACCESS_TOKEN_SECRET"] %>
  nyt_apiKey: <%= ENV["NYT_APIKEY"] %>