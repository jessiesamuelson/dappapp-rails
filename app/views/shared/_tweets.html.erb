<section id="tweets">
  <ul>
  	<% if @tweets %>
	    <% @tweets.each do |tweet| %>
	    	
	      <li><%= link_to tweet["text"], "https://www.twitter.com/#{tweet['user']['screen_name']}" %></li>
	     <% end %>
	  <% end %>
  </ul>
</section>