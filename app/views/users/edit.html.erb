<h1>Editing User</h1>
<%= form_for(@user) do |form| %>
<%= render 'form' %>
  <div class="actions">
    <%= form.submit "Update" %>
  </div>
<% end %>
<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
